import { Container } from "pixi.js";

export class ReelFrameView extends Container {
  constructor() {
    super();
        // Do not draw any frame or graphics.
        // The frame asset will be added later when real assets exist.
  }

    updateFrameSize() {
        // Empty — no frame scaling needed now.
      }
    }
